<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!-- Fired by the self-chaining ~2-minute exact alarm; not exported: only
         our own PendingIntents target it. -->
    <receiver
      android:name="com.kssolar.trackingwatchdog.WatchdogAlarmReceiver"
      android:exported="false" />

    <!-- Re-arms the alarm chain after reboot / app update. System broadcasts
         are delivered to non-exported receivers. -->
    <receiver
      android:name="com.kssolar.trackingwatchdog.WatchdogBootReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
